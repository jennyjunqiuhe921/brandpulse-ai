# ── Core ──────────────────────────────────────────────────────────────────────
streamlit>=1.35.0
anthropic>=0.28.0
python-dotenv>=1.0.0
# 注：chromadb（知识库向量库）从云端依赖中移除——它牵连 opentelemetry/grpc/protobuf
# 一长串脆弱依赖，在 Streamlit Cloud 上反复触发 protobuf 版本冲突。知识库功能在
# rag_engine 中已做惰性容错降级（缺 chromadb 时显示 0/不可用，不影响其余全部功能）。
# 本地开发如需知识库：pip install chromadb 即可恢复。

# ── Document Processing ────────────────────────────────────────────────────────
pypdf>=4.2.0

# ── Web Scraping ───────────────────────────────────────────────────────────────
trafilatura>=1.10.0
beautifulsoup4>=4.12.0

# ── Data & Visualization ───────────────────────────────────────────────────────
pandas>=2.2.0
plotly>=5.22.0

# ── Utilities ─────────────────────────────────────────────────────────────────
requests>=2.32.0

# ── Database & Auth（路线A：持久化存储 + 登录）──────────────────────────────────
SQLAlchemy>=2.0.0
bcrypt>=4.0.0
psycopg2-binary>=2.9.0   # 线上 Postgres（本地默认 SQLite 无需此项也能跑）
streamlit-cookies-controller>=0.0.4   # 登录态持久化（签名token写cookie，刷新/重连不掉登录）
